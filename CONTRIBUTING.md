### Commit messages

Please, add a label before commit message. For example: `feat: add tag`. It means that I added new feature and feature is tag

### Pull requests

Create a new branch and open a pull request to dev branch

### Test

Before open a pull request make sure that in `npm run lint` no warnings and errors.
